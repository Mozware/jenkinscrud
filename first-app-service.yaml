apiVersion: v1
kind: Service
metadata:
  name: demo-jenkins-app-service
spec:
  selector:
    app: demo-jenkins-app
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
